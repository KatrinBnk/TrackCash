# TrackCash

Приложение "TrackCash" — это клиент-серверное веб-приложение для учета личных расходов. 
Разрабатывается с использованием архитектуры MVC и TDD (Test-Driven Development).

Приложение разрабатывается в рамах курсовой работу по курсу "Технологии Веб-программирования" (вариант работы 22)

## Описание
Приложение позволяет:
- **Администраторам**: управлять пользователями, отделами и доступом.
- **Менеджерам**: планировать ресурсы, управлять категориями расходов и просматривать статистику отдела.
- **Сотрудникам**: вести записи о расходах/доходах и просматривать личную статистику.

## Технологии
- **Клиент**: HTML, CSS, JavaScript, jQuery, Mocha, Handlebars(1)
- **Сервер**: Node.js, Express, MySQL, JSON, AJAX, NPM

## Текущий прогресс: Серверная часть

### Готово
- **Аутентификация и регистрация**
    - [x] Регистрация пользователей (только для администраторов)
    - [x] Логин пользователей с выдачей JWT-токена
    - [x] Хеширование паролей с использованием `bcrypt`
    - [x] Middleware для проверки токена и ролей (`admin`, `manager`, `employee`)

- **Транзакции**
    - [x] Добавление транзакций (расходов) сотрудниками (`POST /api/transactions`)
    - [x] Ограничение доступа к добавлению транзакций только для роли `employee`

- **Отделы**
    - [x] Создание отделов администраторами (`POST /api/departments`)
    - [x] Обновление отделов администраторами (`PUT /api/departments/:id`)
        - [x] Переименование отдела
        - [x] Назначение менеджера (с проверкой роли `manager`)
        - [x] Запрет назначения менеджера, уже привязанного к другому отделу

- **Тесты**
    - [x] Разделение тестов на модули (`auth.test.js`, `transactions.test.js`, `departments.test.js`)
    - [x] Общая настройка тестов (`testSetup.js`) с предзаполнением базы данных

### Предстоит сделать
- **Аутентификация и регистрация**
    - [ ] Logout
    - [ ] Регистрация с указанием department_id (сейчас null)

- **Транзакции**
    - [ ] Просмотр личных транзакций сотрудником (`GET /api/transactions`)
    - [ ] Редактирование транзакций сотрудником (`PUT /api/transactions/:id`) (нужно ли разрешать?)
    - [ ] Удаление транзакций сотрудником (`DELETE /api/transactions/:id`) (нужно ли разрешать?)
    - [ ] Валидация `category_id` перед добавлением транзакции

- **Отделы**
    - [ ] Просмотр списка отделов (`GET /api/departments`)
    - [ ] Удаление отдела администратором (`DELETE /api/departments/:id`)

- **Категории (для менеджеров)**
    - [ ] Создание категорий (`POST /api/categories`)
    - [ ] Обновление категорий (`PUT /api/categories/:id`)
    - [ ] Удаление категорий (`DELETE /api/categories/:id`)
    - [ ] Просмотр категорий (`GET /api/categories`)

- **Статистика**
    - [ ] Личная статистика для сотрудников (`GET /api/stats/personal`)
    - [ ] Статистика отдела для менеджеров (`GET /api/stats/department`)

